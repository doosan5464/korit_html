/* 
JavaScript -> 인터프리터, 스크립트 언어 (틀리기 직전까지는 실행함) (파이썬)
           -> 한 줄씩 번역하자마자 실행 (자바 컴파일러는 한 번에)

절대 경로 : 모든 경로를 써야 함

상대 경로 : 현재 있는 경로부터 써야 함 // node ./javascript_study/js/print.js
                                    // code runner 설치하면 쉽게 가능. (오른쪽 위에 실행키 생김, 터미널이 아닌 OUTPUT에서 실행됨)
*/

console.log("hello js"); // 터미널로 실행 powershell(윈도우에 있음), cmd, git-bash(이걸로 할거임) // Ctrl Shift P 해서 Terminal: Select Default Profile로 git-bash를 기본으로
console.log("이름: 최석현")
console.log("나이: 25")
console.error("오류 출력~~~") // 여기서는 티가 안나지만 크롬 콘솔에서는 티가 난다 
// console.loge("성별: 남성") // 여기 오류가 있어도 앞까지 출력이 됨

